const AddCode = () => {
    return (
        <div>
            <h1>add code</h1>
            <input type="text" placeholder="Enter code" />
            {/* how to add icon then? */}
            <button type="submit">Add</button>

    </div>  
    )
}
export default AddCode